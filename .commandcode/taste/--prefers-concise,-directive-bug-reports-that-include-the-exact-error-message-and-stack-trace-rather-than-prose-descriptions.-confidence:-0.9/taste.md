# - Prefers concise, directive bug reports that include the exact error message and stack trace rather than prose descriptions. Confidence: 0.9
- Prefers concise, directive bug reports that include the exact error message and stack trace rather than prose descriptions. Confidence: 0.9
- Reports bugs with the symptom first ("X is not working"), then the error/log, then the file/component reference — i.e., problem → evidence → location. Confidence: 0.9
- Frameworks/tools in stack: Expo SDK 56, React Native 0.85, React 19, expo-router (app/ directory), nativewind (css-interop), react-native-view-shot, React Compiler enabled. Confidence: 0.95
- Uses Android as primary development target (adb available, debug build installed on device). Confidence: 0.9
- Project uses git for version control; uses `git diff` to compare changes during SDK migrations. Confidence: 0.9
- Prefers end-to-end reproduction on a real device (via adb + dev-client + deep link) rather than theoretical analysis or just static reading when debugging runtime issues. Confidence: 0.9
- Uses Metro dev server / `expo start --dev-client` for live debugging. Confidence: 0.85
- Is comfortable with low-level debugging (inspecting node_modules, reading compiled bundle output, running babel transforms locally to inspect compiler output). Confidence: 0.9
- Deep link scheme for the app is "acme". Confidence: 0.7
- App package id: com.surya7314.kriya. Confidence: 0.7
- Prefers an audit/investigate-first workflow: fully diagnose and identify exact issues (e.g., which specific rows are missing) before making any modifications or regenerating data. Confidence: 0.85
- Writes detailed, structured handoff/task documents with explicit sections (Project, Current Status, Next Task, Constraints, Expected Deliverable) rather than free-form instructions. Confidence: 0.8
- Requires scripts to be idempotent and safely resumable if interrupted. Confidence: 0.85
- Prefers conservative, non-destructive changes: do not alter database schema unless necessary, never delete existing data. Confidence: 0.85
- Expects graceful fallback when localized or translated content is unavailable rather than crashing or showing blank screens. Confidence: 0.75
- Project (Kriya) uses a SQLite database at assets/db/gita.db storing Bhagavad Gita verses and commentaries; uses Sarvam AI API (SARVAM_API_KEY env var) for Telugu translation via scripts/translate-telugu.mjs. Confidence: 0.85
- Prefers data-driven, runtime-discovered content over hardcoded lists: new assets (e.g., share backgrounds) uploaded to the R2 store should be picked up by the frontend automatically, without requiring code changes. Confidence: 0.9
- Kriya share-backgrounds workflow (explicitly preferred): adding a background means uploading <id>.jpeg to the R2 backgrounds/ folder AND adding {"id": "<id>"} to backgrounds/index.json — the app discovers backgrounds at runtime from index.json, so no server or code changes are needed. Confidence: 0.9
- Prefers no-server solutions: for remote content discovery, favors static files in R2 (e.g., a metadata index.json fetched at runtime) over building a backend/list endpoint. Confidence: 0.7
- Gets impatient with long-running investigations; wants the agent to stop mid-process and summarize findings/status rather than continue digging ("stop the process, you are taking tooo long"). Prefers time-boxed debugging with quick progress check-ins. Confidence: 0.85
- Prefers to run long-running native builds themselves in their own terminal (e.g., `npx expo run:android`) rather than having the agent execute them; agent should diagnose/setup and hand over the build command ("can i run it myself, npx expo run:android, in another terminal?"). Confidence: 0.7
- Prefers registering fonts centrally in app.json via the expo-font config plugin (fontFamily + fontDefinitions, plus the ios.fonts array) for both Android and iOS, following the project's existing font-registration pattern, rather than ad-hoc native wiring ("hook it up in the app.json for android and ios, like we have done for the rest of the fonts"). Confidence: 0.9
- Prefers the managed Expo workflow: native android/ and ios/ folders are generated artifacts of `npx expo prebuild` and should not be hand-edited (no pbxproj, Info.plist, splash storyboard, or Android res/font changes); configuration should live in app.json only, and any manual native-folder edits should be reverted ("no you don't need to touch splash screens and the native android ios folders, as they will be generated by npx expo prebuild"). Confidence: 0.9
- Prefers localized/Indic text (e.g., Telugu) in the app to use a dedicated font (NTR-Regular) applied consistently across all surfaces that render that language (main screens, share cards, splash screen), not just one component. Confidence: 0.8
- When temporarily disabling a feature, prefers keeping the code in place and commenting it out (with clear markers like `// TELUGU DISABLED`) rather than deleting it, so it can be easily re-enabled later ("keep them in the code, but comment it out"). Confidence: 0.85
- Prefers to defer/park in-progress or partially-built features rather than finishing them now, in order to focus on other priorities ("ill get to them later... i want to focus on other features right now"). Confidence: 0.65
- Prefers configuring native Android build settings (e.g., minSdkVersion) via the expo-build-properties config plugin in app.json rather than editing the generated android/ project or accepting template defaults ("In an Expo project, use expo-build-properties: { android: { minSdkVersion: 26 } }"). Confidence: 0.85
- Diagnoses build failures by reading upstream library documentation for platform requirements (e.g., react-native-webgpu requiring Android API 26+) and quotes the docs' exact requirement + prescribed fix when reporting the issue. Confidence: 0.7
- When trying out a new library/effect, prefers a dedicated in-app test page (e.g., app/effects-test.tsx) reachable from the home screen via a header icon button beside existing icons, rather than replacing production UI ("create a test page for me beside the calendar icon on the index.tsx page, where i can test these effects"). Confidence: 0.75
- Provides ready-to-use code snippets (imports + usage with exact prop values) as the spec for a feature request and expects them implemented as-is/verbatim rather than re-designed or re-interpreted. Confidence: 0.7
- Likes to keep the repo free of one-off debug artifacts and stale intermediate files (e.g., babel-inspection dumps like share2.pretty.js / share2.compiled.js, throwaway transform scripts like tmp-transform.cjs, or gradle diagnostics reports) — asks to confirm whether such files are still needed and has them removed when nothing references or imports them (they also break `tsc --noEmit`). Confidence: 0.8
- Prefers dependency hygiene: wants to audit heavy libraries/frameworks (e.g., gluestack-ui, NativeWind) and remove those that aren't doing much, replacing bloaty wrapper components (a Spinner that's just ActivityIndicator) with simpler standard React Native equivalents and uninstalling the now-unused packages rather than keeping them wired up ("do we need nativewind and gluestack ui? I want to see if we can get rid of them if they arent doing much"). Gives conditional approval to execute such removals provided the app behavior is unaffected ("okay go ahead if it doesnt affect the app much"). Confidence: 0.8
- When abandoning an experimental library/feature, expects a complete clean rollback — uninstall the package, delete its test files/routes, revert its config changes, and restore the last known-good state — while surgically preserving unrelated in-progress work mixed into the same working tree, since a plain git revert can't separate interleaved changes ("both of these changes are being tracked at once, so i cannot just go back to the before telugu one"). Confidence: 0.85
- Treats features parked for later differently from experiments abandoned for good: code meant to be revisited gets commented out with markers and left in place, while rejected libraries get fully deleted/uninstalled ("keep them in the code, but comment it out" vs "i dont want to use this... delete that package, and go back to how the codebase was"). Confidence: 0.7
- When abandoning an experimental library/feature, expects a complete clean rollback — uninstall the package, delete its test files/routes, revert its config changes, and restore the last known-good state — while surgically preserving unrelated in-progress work mixed into the same working tree, since a plain git revert can't separate interleaved changes ("both of these changes are being tracked at once, so i cannot just go back to the before telugu one"). Confidence: 0.85
- Treats features parked for later differently from experiments abandoned for good: code meant to be revisited gets commented out with markers and left in place, while rejected libraries get fully deleted/uninstalled ("keep them in the code, but comment it out" vs "i dont want to use this... delete that package, and go back to how the codebase was"). Confidence: 0.7
